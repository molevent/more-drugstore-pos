# 🚀 คู่มือตั้งค่า Supabase แบบครบถ้วน

## ขั้นตอนที่ 1: รัน SQL Script

1. **เปิด Supabase Dashboard**
   - ไปที่: https://supabase.com/dashboard
   - เลือก Project: **tqbonqjabeavlwjvrpqx**

2. **เปิด SQL Editor**
   - คลิกเมนู **SQL Editor** ทางซ้าย
   - คลิก **New query**

3. **Copy และ Paste SQL Script**
   - เปิดไฟล์: `supabase/COMPLETE_SETUP.sql`
   - Copy ทั้งหมด
   - Paste ลงใน SQL Editor

4. **รัน Script**
   - คลิกปุ่ม **▶️ RUN** (หรือกด Ctrl+Enter)
   - รอจนเสร็จ (ประมาณ 10-30 วินาที)
   - ถ้าเห็น "Success" แสดงว่าสำเร็จ

---

## ขั้นตอนที่ 2: สร้าง Storage Bucket สำหรับรูปภาพ

1. **ไปที่ Storage**
   - คลิกเมนู **Storage** ทางซ้าย

2. **สร้าง Bucket ใหม่**
   - คลิก **New bucket**
   - ชื่อ: `products`
   - Public bucket: ✅ เลือก (เพื่อให้เข้าถึงรูปได้)
   - คลิก **Create bucket**

3. **ตั้งค่า Policies**
   - คลิกที่ bucket `products`
   - ไปที่แท็บ **Policies**
   - คลิก **New policy**
   - เลือก **For full customization**
   - ใส่ชื่อ: `Allow authenticated uploads`
   - Policy definition:
   ```sql
   (bucket_id = 'products'::text) AND (auth.role() = 'authenticated'::text)
   ```
   - เลือก operations: INSERT, UPDATE, SELECT
   - คลิก **Review** → **Save policy**

---

## ขั้นตอนที่ 3: สร้าง Admin User

### 3.1 สร้าง User ใน Authentication

1. **ไปที่ Authentication**
   - คลิกเมนู **Authentication** ทางซ้าย
   - คลิกแท็บ **Users**

2. **เพิ่ม User ใหม่**
   - คลิก **Add user** → **Create new user**
   - Email: `admin@moredrug.com`
   - Password: `Admin@123` (หรือรหัสที่คุณต้องการ)
   - ✅ เลือก **Auto Confirm User**
   - คลิก **Create user**

3. **Copy User ID**
   - หลังสร้างเสร็จ จะเห็น User ในรายการ
   - คลิกที่ User
   - Copy **User UID** (เช่น: `a1b2c3d4-...`)

### 3.2 สร้าง User Profile

1. **กลับไปที่ SQL Editor**
   - คลิกเมนู **SQL Editor**
   - **New query**

2. **รัน SQL นี้** (แทน `YOUR_USER_ID` ด้วย User UID ที่ copy ไว้):
   ```sql
   INSERT INTO public.users (id, email, full_name, role, language)
   VALUES (
     'YOUR_USER_ID',
     'admin@moredrug.com',
     'Admin User',
     'admin',
     'th'
   );
   ```

3. **คลิก RUN**

---

## ขั้นตอนที่ 4: ทดสอบระบบ

1. **เปิดแอป**
   - ไปที่: http://localhost:5173
   - หรือ: https://your-app.netlify.app

2. **Login**
   - Email: `admin@moredrug.com`
   - Password: `Admin@123` (หรือที่คุณตั้งไว้)

3. **ทดสอบฟีเจอร์**
   - ✅ Dashboard แสดงข้อมูล
   - ✅ หน้าหมวดหมู่ - เพิ่ม/แก้ไข/ลบได้
   - ✅ หน้าสินค้า - เพิ่ม/แก้ไข/อัปโหลดรูปได้
   - ✅ POS - ค้นหาสินค้าได้

---

## ❗ แก้ปัญหาที่พบบ่อย

### ปัญหา: ไม่สามารถ Login ได้
**วิธีแก้:**
1. ตรวจสอบว่า User ถูก Auto Confirm แล้ว
2. ตรวจสอบว่ามี User Profile ใน `public.users` แล้ว
3. ลอง Reset Password ใน Authentication

### ปัญหา: ไม่เห็นสินค้า
**วิธีแก้:**
1. ตรวจสอบว่ารัน SQL Script เรียบร้อยแล้ว
2. ตรวจสอบ RLS Policies ว่าเปิดใช้งานแล้ว
3. ลองเพิ่มสินค้าใหม่ทดสอบ

### ปัญหา: อัปโหลดรูปไม่ได้
**วิธีแก้:**
1. ตรวจสอบว่าสร้าง bucket `products` แล้ว
2. ตรวจสอบว่า bucket เป็น Public
3. ตรวจสอบ Storage Policies

---

## 📝 สรุป

หลังทำตามขั้นตอนทั้งหมด คุณจะได้:

✅ Database พร้อม 13 tables
✅ RLS Policies ครบถ้วน
✅ Functions และ Triggers
✅ Storage สำหรับรูปภาพ
✅ Admin User พร้อมใช้งาน
✅ ข้อมูลเริ่มต้น (categories, platforms)

**ระบบพร้อมใช้งานเต็มรูปแบบแล้ว!** 🎉
