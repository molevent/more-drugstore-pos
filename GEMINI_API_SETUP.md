# 🤖 คู่มือตั้งค่า Google Gemini API สำหรับ AI ช่วยแนะนำยา

## ขั้นตอนที่ 1: สมัคร Google Gemini API Key (ฟรี)

1. **ไปที่เว็บไซต์ Google AI Studio**
   - เปิด: https://makersuite.google.com/app/apikey
   - หรือ: https://aistudio.google.com/app/apikey

2. **Login ด้วย Google Account**
   - ใช้ Gmail ของคุณ

3. **สร้าง API Key**
   - คลิก **"Create API Key"** หรือ **"Get API Key"**
   - เลือก **"Create API key in new project"**
   - รอสักครู่ (ประมาณ 5-10 วินาที)

4. **Copy API Key**
   - คุณจะเห็น API Key ยาวๆ เช่น: `AIzaSyD...`
   - คลิก **Copy** เก็บไว้

---

## ขั้นตอนที่ 2: ตั้งค่า API Key ในโปรเจค

### **สำหรับ Local Development (localhost)**

1. **สร้างไฟล์ `.env`** ในโฟลเดอร์หลัก
   ```bash
   # ที่ /Users/surecomputer/Desktop/More Drugstore App/
   ```

2. **เปิดไฟล์ `.env`** และใส่:
   ```env
   VITE_SUPABASE_URL=https://tqbonqjabeavlwjvrpqx.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   VITE_GEMINI_API_KEY=AIzaSyD... (ใส่ API Key ที่ copy ไว้)
   ```

3. **บันทึกไฟล์**

4. **Restart dev server**
   ```bash
   # กด Ctrl+C เพื่อหยุด server
   npm run dev
   ```

---

### **สำหรับ Production (Netlify)**

1. **เปิด Netlify Dashboard**
   - ไปที่: https://app.netlify.com
   - เลือก site ของคุณ

2. **ไปที่ Site settings**
   - คลิก **Site settings** (ด้านบน)

3. **เพิ่ม Environment Variables**
   - คลิกเมนู **Environment variables** (ทางซ้าย)
   - คลิก **Add a variable**
   - ใส่:
     - **Key**: `VITE_GEMINI_API_KEY`
     - **Value**: `AIzaSyD...` (API Key ของคุณ)
   - คลิก **Save**

4. **Redeploy site**
   - ไปที่แท็บ **Deploys**
   - คลิก **Trigger deploy** → **Clear cache and deploy site**

---

## ขั้นตอนที่ 3: ทดสอบ

1. **เปิดแอป**
   - Local: http://localhost:5173
   - Production: https://your-app.netlify.app

2. **ไปที่ "AI ช่วยแนะนำยา"**
   - คลิกเมนูทางซ้าย

3. **กรอกข้อมูลทดสอบ:**
   - อายุ: 30
   - ไม่ท้อง
   - อาการ: "ปวดหัว มีไข้ เมื่อยตัว"
   - คลิก **"วิเคราะห์และแนะนำยา"**

4. **ตรวจสอบผลลัพธ์:**
   - ✅ ถ้าทำงาน: จะเห็นยาที่แนะนำ
   - ❌ ถ้าเกิด error: ตรวจสอบ API Key

---

## 💰 ค่าใช้จ่าย (ฟรี!)

**Google Gemini API - Free Tier:**
- ✅ **60 requests/minute** (ฟรี)
- ✅ **1,500 requests/day** (ฟรี)
- ✅ **ไม่ต้องใส่บัตรเครดิต**
- ✅ **ใช้ได้ตลอดชีพ**

**เพียงพอสำหรับร้านขายยาหรือไม่?**
- ร้านเล็ก (10-20 ลูกค้า/วัน): ✅ เพียงพอมาก
- ร้านกลาง (50-100 ลูกค้า/วัน): ✅ เพียงพอ
- ร้านใหญ่ (200+ ลูกค้า/วัน): ⚠️ อาจต้อง upgrade

---

## ❗ แก้ปัญหาที่พบบ่อย

### ปัญหา 1: "Please set Google Gemini API Key in .env"
**วิธีแก้:**
1. ตรวจสอบว่าสร้างไฟล์ `.env` แล้ว
2. ตรวจสอบว่าใส่ `VITE_GEMINI_API_KEY=...` ถูกต้อง
3. Restart dev server (Ctrl+C แล้ว `npm run dev`)

### ปัญหา 2: "API Key not valid"
**วิธีแก้:**
1. ตรวจสอบว่า copy API Key ครบถ้วน
2. ลองสร้าง API Key ใหม่
3. ตรวจสอบว่าไม่มีช่องว่างหน้า-หลัง API Key

### ปัญหา 3: "Quota exceeded"
**วิธีแก้:**
1. รอ 1 นาที (reset ทุก 1 นาที)
2. หรือรอถึงวันพรุ่งนี้ (reset ทุก 24 ชั่วโมง)
3. หรือ upgrade เป็น paid plan

### ปัญหา 4: ใช้งานได้บน localhost แต่ไม่ได้บน Netlify
**วิธีแก้:**
1. ตรวจสอบว่าเพิ่ม Environment Variable ใน Netlify แล้ว
2. ตรวจสอบว่าชื่อตัวแปรถูกต้อง: `VITE_GEMINI_API_KEY`
3. Redeploy site

---

## 🔒 ความปลอดภัย

**ข้อควรระวัง:**
- ❌ **อย่า** commit ไฟล์ `.env` ลง Git
- ❌ **อย่า** แชร์ API Key ให้คนอื่น
- ✅ **ควร** เก็บ API Key ไว้เป็นความลับ
- ✅ **ควร** ใช้ Environment Variables

**ไฟล์ `.gitignore` ได้ป้องกันแล้ว:**
```
.env
.env.local
```

---

## 📊 ตรวจสอบการใช้งาน

**ดู Usage ของ API:**
1. ไปที่: https://aistudio.google.com/app/apikey
2. คลิกที่ API Key ของคุณ
3. ดู **Usage** และ **Quota**

---

## ✅ Checklist

ก่อนใช้งาน ตรวจสอบว่า:
- [ ] สมัคร Google Gemini API Key แล้ว
- [ ] Copy API Key เก็บไว้แล้ว
- [ ] สร้างไฟล์ `.env` และใส่ API Key แล้ว
- [ ] Restart dev server แล้ว
- [ ] ทดสอบการทำงานแล้ว (ใช้งานได้)

---

## 🎉 เสร็จแล้ว!

ตอนนี้คุณสามารถใช้ **AI ช่วยแนะนำยา** ได้แล้ว!

**ถ้ามีปัญหา บอกผมได้เลยครับ!** 💪
